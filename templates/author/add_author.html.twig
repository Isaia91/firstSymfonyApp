{% extends 'base.html.twig' %}

{% block title %}Ajouter un book{% endblock %}

{% block body %}

    <div class="container mt-4">
        <h1 class="mb-4">Formulaire d'ajout auteur</h1>
        <div class="row">
            <div class="col-md-4">
                {{ form(formulaire_author) }}
            </div>
        </div>
        <div class="row">
            <div class="col-md-4">
                {% if baseUrl %}
                    <a href="{{ baseUrl }}" class="btn btn-secondary my-2">Accueil</a>
                {% endif %}
            </div>
        </div>
    </div>


{% endblock %}